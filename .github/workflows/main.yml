name: hello-world
on: workflow_dispatch
jobs:
  install:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Install dependencies
        uses: actions/setup-node@v4
        with:
          node-version: 20.8.1
      - run: npm ci
    
  cypress-run-all-tests:
    runs-on: ubuntu-latest
    steps:
      - name: my-step
        run: echo "Cypress Test Hello World!"
